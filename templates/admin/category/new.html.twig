{% extends 'admin/base.html.twig' %}

{% block title %}New Category
{% endblock %}

{% block body %}
	<div class="container my-3">
		<h1 class="text-black text-center">CATEGORY MANAGEMENT</h1>
		{{form_start(form)}}
		{{form_widget(form)}}
		<button type="submit" class="btn btn-primary  w-100">
			<i class="fa-solid fa-check"></i>Add</button>
		{{form_end(form)}}
	</div>
{% endblock %}
